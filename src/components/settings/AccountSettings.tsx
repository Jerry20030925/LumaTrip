import React from 'react';

const AccountSettings: React.FC = () => {
  return (
    <div>
      <h3>Account Settings</h3>
      {/* Form for changing email, password, etc. */}
    </div>
  );
};

export default AccountSettings;