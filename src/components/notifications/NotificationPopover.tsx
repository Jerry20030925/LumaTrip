import React from 'react';

const NotificationPopover: React.FC = () => {
  return (
    <div>
      <h4>Notifications Popover</h4>
      {/* Popover content with a list of recent notifications */}
    </div>
  );
};

export default NotificationPopover;