import React from 'react';

const ProfileTabs: React.FC = () => {
  return (
    <div>
      {/* Tabs for posts, saved, tagged, etc. */}
      <button>Posts</button>
      <button>Saved</button>
    </div>
  );
};

export default ProfileTabs;